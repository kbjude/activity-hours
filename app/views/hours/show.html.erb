<h1> <%= totalhours %> </h1>

<ul>
<% @hours.each do |hour| %>
    <li> <%= hour.description %> </li>
    <li> <%= hour.hours %> </li>
    <li> <%= hour.group.icon %> </li>
    <li> <%= hour.created_at %> </li>
<% end %>
</ul>

<%= link_to 'Add new', new_hour_path %>